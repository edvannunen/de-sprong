# Copy this file to .env and fill in real values before running `npm run db:seed-users`.
# Use long, random passwords — this app has no login rate-limiting.

ADMIN_USERNAME=
ADMIN_PASSWORD=

GUEST_USERNAME=
GUEST_PASSWORD=
